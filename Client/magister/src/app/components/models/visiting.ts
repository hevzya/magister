export interface VisitingModel {
    id?: string,
    isPresent?: string,
    isLate?: string,
    lessonMark?: number,
    examMark?: number,

    studentId?: string,
    lessonId?: string,

    studentName?: string
}